import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  formWrapper: {
    paddingTop: '60px',
  },
  textField: {
    maxWidth: 600,
  },
  root: {
    marginBottom: 24,
    minWidth: 600,
    display: 'flex',
  },
});
